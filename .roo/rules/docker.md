# Docker Usage

This project must be run using Docker. Never attempt to run Python commands locally.
All development and execution should happen within the Docker container.

Use `docker compose` with space, not with dash